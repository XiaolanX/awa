name: PixelOS_Ultimate_GKI_V18_FAST

on:
  workflow_dispatch:

jobs:
  Build_Kernel:
    name: Build GKI with SukiSU-FAST Mode
    runs-on: ubuntu-latest
    
    steps:
      - name: Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'

      - name: Setup Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison binfmt-support libssl-dev libelf-dev clang llvm lld python3-pip git-lfs gawk dos2unix rsync libncurses-dev jq
          pip3 install setuptools

      - name: Clone Kernel Source
        run: |
          # 克隆一加 12R 源码
          git clone https://github.com/OnePlus12R-development/android_kernel_oneplus_sm8550.git --depth=1 -b sixteen-qpr1 kernel/sm8550

      - name: Get SUSFS & KPM
        run: |
          # 参考参考代码 source: 113, 116
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android14-6.1 susfs4ksu_src
          mkdir -p kernel/sm8550/drivers/kpm
          curl -LSs https://raw.githubusercontent.com/rifsxd/KPM/main/kpm/Kbuild -o kernel/sm8550/drivers/kpm/Kbuild
          curl -LSs https://raw.githubusercontent.com/rifsxd/KPM/main/kpm/kpm.c -o kernel/sm8550/drivers/kpm/kpm.c

      - name: Surgical Integration & Oplus Fix
        run: |
          cd kernel/sm8550
          
          echo "--- 1. 参考清理 ABI 导出限制 ---"
          # 参考 source: 61，物理删除受保护的导出列表限制
          find . -name "BUILD.bazel" -exec sed -i '/"protected_exports_list"/d' {} +
          rm -f android/abi_gki_protected_exports_* 2>/dev/null || true

          echo "--- 2. 参考移除构建配置审查 ---"
          # 参考 source: 172，移除 check_defconfig 防止中断
          [ -f "./build.config.gki" ] && sed -i 's/check_defconfig//' ./build.config.gki || true

          echo "--- 3. 深度清理失效的 Kconfig 引用 ---"
          grep -r "source" . -l | xargs -I {} python3 -c '
          import os, re, sys
          f_path = "{}"
          with open(f_path, "r") as f: lines = f.readlines()
          with open(f_path, "w") as f:
              for line in lines:
                  match = re.search(r"source\s+\"([^\"]+)\"", line)
                  if match:
                      target = match.group(1)
                      if not os.path.exists(os.path.join(os.path.dirname(f_path), target)): continue
                  f.write(line)
          ' || true

          echo "--- 4. 参考物理注入 SUSFS 文件 ---"
          # 参考 source: 116，拷贝 fs 和 include 目录
          cp ../../susfs4ksu_src/kernel_patches/fs/* ./fs/
          cp ../../susfs4ksu_src/kernel_patches/include/linux/* ./include/linux/
          PATCH_FILE=$(find ../../susfs4ksu_src/kernel_patches/ -name "*add_susfs_in_gki-android14-6.1.patch" | head -n 1)
          patch -p1 < "$PATCH_FILE" || echo "Patch conflict ignored."

          echo "--- 5. 注入 KPM ---"
          if ! grep -q "obj-y += kpm/" drivers/Makefile; then
            echo "obj-y += kpm/" >> drivers/Makefile
          fi

      - name: Auto-Detect Clang and Tools
        id: clang_detect
        run: |
          cd kernel/sm8550
          # 参考 source: 187-192 的逻辑解析 Clang 版本
          CLANG_VERSION=$(grep '^CLANG_VERSION=' ./build.config.constants | cut -d'=' -f2 || echo "r487703")
          echo "检测到目标 Clang 版本: $CLANG_VERSION"
          
          # 下载官方 AOSP Clang
          mkdir -p $GITHUB_WORKSPACE/prebuilts/clang/host/linux-x86/clang-aosp
          wget https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/$CLANG_VERSION.tar.gz
          tar -xzf $CLANG_VERSION.tar.gz -C $GITHUB_WORKSPACE/prebuilts/clang/host/linux-x86/clang-aosp
          
          # 设置环境变量
          echo "CLANG_BIN=$GITHUB_WORKSPACE/prebuilts/clang/host/linux-x86/clang-aosp/bin" >> $GITHUB_ENV

      - name: Build GKI Kernel (FAST_BUILD)
        run: |
          cd kernel/sm8550
          export PATH="${{ env.CLANG_BIN }}:$PATH"
          
          GKI_CONF="arch/arm64/configs/gki_defconfig"
          
          # 注入参考配置 source: 160, 183
          echo "--- 配置 ThinLTO 与 SUSFS 全家桶 ---"
          sed -i 's/^CONFIG_LTO_CLANG_FULL=y/CONFIG_LTO_CLANG_THIN=y/' "$GKI_CONF"
          {
            echo "CONFIG_KSU=y"
            echo "CONFIG_KPM=y"
            echo "CONFIG_KSU_SUSFS=y"
            echo "CONFIG_KSU_SUSFS_SUS_PATH=y"
            echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y"
            echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y"
            echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y"
          } >> "$GKI_CONF"
          
          # 参考 source: 193-194 设定编译参数
          export MAKE_ARGS="ARCH=arm64 LLVM=1 LLVM_IAS=1 \
                           CROSS_COMPILE=aarch64-linux-gnu- \
                           LD=ld.lld NM=llvm-nm OBJCOPY=llvm-objcopy \
                           OBJDUMP=llvm-objdump STRIP=llvm-strip \
                           KCFLAGS=-Wno-error"
          
          make O=out $MAKE_ARGS gki_defconfig
          make -j$(nproc) O=out $MAKE_ARGS Image.gz
          
          if [ -f "out/arch/arm64/boot/Image.gz" ]; then
            mv out/arch/arm64/boot/Image.gz ${GITHUB_WORKSPACE}/PixelOS-Ultimate-GKI.gz
          else
            exit 1
          fi

      - name: Upload Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: "GKI-FAST-${{ github.run_id }}"
          name: "OnePlus 12R FAST GKI (Final V18)"
          files: |
            *.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
